# Web Frontend Workspace

This top-level `web/` directory is reserved for the ETL web UI refactor.

Suggested layout:
- `pages/`: page-level HTML entry files
- `assets/js/`: shared JavaScript bundles
- `assets/css/`: shared stylesheets

The backend currently lives under `etl/web_api.py`; this directory will hold extracted frontend files.
